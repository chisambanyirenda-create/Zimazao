auto-install-peers=false
strict-peer-dependencies=false
# Don't hard-fail the install when a dependency's optional build script is
# skipped — the prebuilt platform binaries cover what we need on Windows.
strict-dep-builds=false
